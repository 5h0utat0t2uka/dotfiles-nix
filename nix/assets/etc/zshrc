# /etc/zshrc: managed by nix-darwin (dotfiles are managed by chezmoi)
# This file is read for interactive shells.

# Only execute this file once per shell.
if [ -n "$__ETC_ZSHRC_SOURCED" -o -n "$NOSYSZSHRC" ]; then return; fi
__ETC_ZSHRC_SOURCED=1

# Do not run promptinit/compinit here.
# User configuration lives in $ZDOTDIR/.zshrc (managed by chezmoi).

# Read system-wide modifications if present.
if test -f /etc/zshrc.local; then
  source /etc/zshrc.local
fi
