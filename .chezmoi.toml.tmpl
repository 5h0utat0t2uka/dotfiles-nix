[data]
username = "{{ .chezmoi.username }}"
hostname = "{{ .chezmoi.hostname }}"
system = "{{ .chezmoi.os }}-{{ .chezmoi.arch }}"

[edit]
command = "/etc/profiles/per-user/shouta/bin/zeditor"
args = ["--wait"]

[git]
autoCommit = true
autoPush = false
